```mermaid
classDiagram
    class Telecommande {
        - lampes : List~Lampe~
        - hifis : List~Hifi~
        + ajouterLampe(l: Lampe)
        + activerLampe(indice: int)
        + desactiverLampe(indice: int)
        + ajouterHifi(h: Hifi)
        + activerHifi(indice: int)
        + desactiverHifi(indice: int)
        + activerTout()
    }

    class Lampe {
        + allumer()
        + eteindre()
    }

    class Hifi {
        + allumer()
        + eteindre()
    }

    Telecommande "1" --> "*" Lampe : contrôle
    Telecommande "1" --> "*" Hifi : contrôle